# =====================================================
#      متطلبات خادم MT5 الوسيط
#      MT5 Middleware Server Requirements
# =====================================================

# إطار العمل الرئيسي - FastAPI
fastapi>=0.104.0

# خادم ASGI لتشغيل FastAPI
uvicorn[standard]>=0.24.0

# مكتبة MetaTrader 5 الرسمية (Windows فقط)
# ملاحظة: هذه المكتبة تعمل فقط على نظام Windows
# MetaTrader5>=5.0.45

# التحقق من صحة البيانات
pydantic>=2.5.0

# دعم HTTP غير متزامن (اختياري)
httpx>=0.25.0

# معالجة الملفات (مدمج مع Python)
# configparser - مدمج

# =====================================================
#      ملاحظات التثبيت:
# =====================================================
#
# 1. تثبيت المتطلبات الأساسية:
#    pip install -r requirements.txt
#
# 2. تثبيت مكتبة MT5 (على Windows فقط):
#    pip install MetaTrader5
#
# 3. تشغيل الخادم:
#    python main.py
#    أو
#    uvicorn main:app --reload --port 8000
#
# =====================================================
