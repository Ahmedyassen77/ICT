# =====================================================
#      متطلبات خادم MT5 الوسيط - الإصدار النهائي
#      MT5 Ultimate Control System Requirements
# =====================================================

# === إطار العمل الرئيسي ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
httpx>=0.25.0

# === التحكم الذكي في الواجهة (الأفضل!) ===
# pywinauto يتحكم في MT5 مثل ما يتحكم Playwright في المتصفح!
# - لا يحتاج صور شاشة
# - يقرأ العناصر مباشرة
# - أسرع وأدق بكثير
pywinauto>=0.6.8
comtypes>=1.2.0

# === التحكم الاحتياطي (بالصور) ===
# هذه المكتبات للحالات الخاصة فقط
pyautogui>=0.9.54
pygetwindow>=0.0.9
pillow>=10.0.0
pyperclip>=1.8.2

# === مكتبة MT5 الرسمية (Windows فقط) ===
# ملاحظة: تعمل فقط على Windows
# ثبتها يدوياً: pip install MetaTrader5
# MetaTrader5>=5.0.45

# =====================================================
#      ملاحظات التثبيت:
# =====================================================
#
# 1. تثبيت كل المتطلبات:
#    pip install -r requirements.txt
#
# 2. تثبيت مكتبة MT5 (Windows فقط):
#    pip install MetaTrader5
#
# 3. تشغيل النظام النهائي (الأفضل!):
#    python mt5_ultimate_control.py
#
# 4. أو تشغيل الخادم الأساسي:
#    python main.py
#
# =====================================================
#      الفرق بين الملفات:
# =====================================================
#
# mt5_ultimate_control.py (الأفضل! ⭐):
#   - نظام التحكم النهائي الذكي
#   - يجمع 4 طرق للتحكم
#   - pywinauto للتحكم الذكي (بدون صور!)
#   - MT5 API للتداول
#   - INI files للـ Backtest
#   - PyAutoGUI احتياطي
#
# mt5_full_control.py:
#   - نظام التحكم بالصور
#   - يستخدم pyautogui فقط
#   - يحتاج صور شاشة
#
# main.py:
#   - الخادم الأساسي
#   - يستخدم Python MT5 API فقط
#   - Backtest عبر ملف .ini
#
# =====================================================
#      طرق التحكم في mt5_ultimate_control.py:
# =====================================================
#
# 1️⃣ pywinauto (الأفضل!):
#    - يقرأ عناصر الواجهة مباشرة
#    - مثل DOM في المتصفح
#    - لا يحتاج صور
#
# 2️⃣ MT5 Python API:
#    - تداول حقيقي
#    - معلومات الحساب
#    - الأسعار الحية
#
# 3️⃣ INI Files:
#    - تشغيل Strategy Tester تلقائياً
#    - أسرع طريقة للـ Backtest
#
# 4️⃣ PyAutoGUI:
#    - احتياطي للحالات الخاصة
#    - صور الشاشة
#
# =====================================================
