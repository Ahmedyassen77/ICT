# =====================================================
#      متطلبات خادم MT5 الوسيط - الإصدار الكامل
#      MT5 Middleware Server Requirements - Full Version
# =====================================================

# === إطار العمل الرئيسي ===
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
httpx>=0.25.0

# === التحكم في الواجهة (للتحكم الكامل) ===
# هذه المكتبات تمكّن AI من التحكم في MT5 مثل الإنسان
pyautogui>=0.9.54
pygetwindow>=0.0.9
pillow>=10.0.0
pyperclip>=1.8.2

# === مكتبة MT5 الرسمية (Windows فقط) ===
# ملاحظة: تعمل فقط على Windows
# ثبتها يدوياً: pip install MetaTrader5
# MetaTrader5>=5.0.45

# =====================================================
#      ملاحظات التثبيت:
# =====================================================
#
# 1. تثبيت كل المتطلبات:
#    pip install -r requirements.txt
#
# 2. تثبيت مكتبة MT5 (Windows فقط):
#    pip install MetaTrader5
#
# 3. تشغيل الخادم الأساسي:
#    python main.py
#
# 4. تشغيل نظام التحكم الكامل:
#    python mt5_full_control.py
#
# =====================================================
#      الفرق بين الملفين:
# =====================================================
#
# main.py:
#   - الخادم الأساسي
#   - يستخدم Python MT5 API فقط
#   - Backtest عبر ملف .ini (غير مرئي)
#
# mt5_full_control.py:
#   - نظام التحكم الكامل
#   - يتحكم في الواجهة مثل الإنسان
#   - Backtest مرئي
#   - التقاط الشاشة
#   - كتابة Expert Advisors
#   - تحكم كامل بالماوس ولوحة المفاتيح
#
# =====================================================
