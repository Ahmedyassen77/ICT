# 🌐 دليل إعداد Cloud Sync الكامل
# التحكم في MetaTrader 5 من أي مكان في العالم

---

## 📋 فهرس المحتويات

1. [ما هو Cloud Sync؟](#-ما-هو-cloud-sync)
2. [كيف يعمل النظام؟](#-كيف-يعمل-النظام)
3. [المتطلبات](#-المتطلبات)
4. [خطوات الإعداد](#-خطوات-الإعداد)
5. [الاستخدام اليومي](#-الاستخدام-اليومي)
6. [الأوامر المتاحة](#-الأوامر-المتاحة)
7. [حل المشاكل](#-حل-المشاكل)
8. [الأمان](#-الأمان)

---

## 🎯 ما هو Cloud Sync؟

**Cloud Sync** هو نظام يسمح لك بالتحكم في MetaTrader 5 من أي مكان:

```
📱 موبايلك (في أي مكان)
       │
       │ تتكلم مع Claude
       ▼
☁️ الإنترنت
       │
       │ HTTP Request
       ▼
💻 جهازك (في البيت/المكتب)
       │
       │ ينفذ الأوامر
       ▼
📊 MetaTrader 5
       │
       │ يعمل EA + Backtest
       ▼
👀 تشاهد النتيجة على شاشتك!
```

### ✨ المميزات:
- ✅ تحكم كامل من الموبايل
- ✅ إنشاء Expert Advisors بأي استراتيجية
- ✅ تشغيل Backtest مرئي
- ✅ قراءة وتحليل النتائج
- ✅ آمن ومحمي بـ API Key
- ✅ بدون تدخل يدوي (بعد الإعداد)

---

## 🔄 كيف يعمل النظام؟

### المكونات الرئيسية:

```
┌─────────────────────────────────────────────────────────────────┐
│                                                                 │
│  1. remote_control_server.py                                    │
│     └── خادم FastAPI يعمل على جهازك                             │
│     └── يستقبل الأوامر وينفذها على MT5                          │
│                                                                 │
│  2. ngrok                                                       │
│     └── يحول localhost إلى رابط عام                             │
│     └── مثال: https://abc123.ngrok.io                          │
│                                                                 │
│  3. mt5_complete_automation.py                                  │
│     └── ينفذ العمليات على MT5                                   │
│     └── إنشاء EA، ترجمة، Backtest، قراءة النتائج                │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### تدفق العمل:

```
أنت: "اعمل EA باستراتيجية RSI"
         │
         ▼
┌─────────────────┐
│   Claude (AI)   │ ← يفهم طلبك
└────────┬────────┘
         │
         ▼ يرسل HTTP POST
┌─────────────────┐
│     ngrok       │ ← https://abc123.ngrok.io
└────────┬────────┘
         │
         ▼ يمرر للخادم المحلي
┌─────────────────┐
│ remote_control  │ ← localhost:8000
│    _server.py   │
└────────┬────────┘
         │
         ▼ يستدعي
┌─────────────────┐
│ mt5_complete_   │
│ automation.py   │
└────────┬────────┘
         │
         ▼ ينفذ على
┌─────────────────┐
│  MetaTrader 5   │
│  • ينشئ EA      │
│  • يترجمه      │
│  • يشغل Backtest│
└─────────────────┘
         │
         ▼
👀 تشاهد النتيجة على شاشتك!
```

---

## 📦 المتطلبات

### على جهازك (Windows):

| المتطلب | الوصف | الرابط |
|---------|-------|--------|
| **Windows 10/11** | نظام التشغيل | - |
| **Python 3.8+** | لغة البرمجة | [python.org](https://python.org) |
| **MetaTrader 5** | منصة التداول | من الوسيط |
| **ngrok** | للرابط العام | [ngrok.com](https://ngrok.com) |
| **Git** | لتحميل المشروع | [git-scm.com](https://git-scm.com) |

### مكتبات Python:
```
fastapi
uvicorn
pydantic
httpx
pywinauto
comtypes
MetaTrader5
pyautogui
pygetwindow
pillow
```

---

## 🚀 خطوات الإعداد

### الخطوة 1: تحميل المشروع

```bash
# افتح Command Prompt أو PowerShell
git clone https://github.com/Ahmedyassen77/ICT.git
cd ICT/mt5_middleware
```

### الخطوة 2: تشغيل سكربت الإعداد التلقائي

```bash
# طريقة سهلة - ضغطة واحدة!
python setup_cloud_sync.py
```

**أو يدوياً:**

```bash
# تثبيت المتطلبات
pip install -r requirements.txt

# تثبيت MetaTrader5 (إذا لم يكن مثبتاً)
pip install MetaTrader5
```

### الخطوة 3: إعداد ngrok

1. **حمّل ngrok** من [ngrok.com/download](https://ngrok.com/download)

2. **سجّل حساب مجاني** على [dashboard.ngrok.com/signup](https://dashboard.ngrok.com/signup)

3. **احصل على Auth Token** من [dashboard.ngrok.com/get-started/your-authtoken](https://dashboard.ngrok.com/get-started/your-authtoken)

4. **فعّل ngrok:**
```bash
ngrok config add-authtoken YOUR_AUTH_TOKEN
```

### الخطوة 4: تشغيل الخادم

```bash
# نافذة 1: شغّل الخادم
python remote_control_server.py
```

**سترى:**
```
╔══════════════════════════════════════════════════════════════╗
║               🌍 MT5 Remote Control Server                   ║
║   🚀 الخادم يعمل على: http://localhost:8000                 ║
║   📚 التوثيق: http://localhost:8000/docs                     ║
╚══════════════════════════════════════════════════════════════╝
```

### الخطوة 5: تشغيل ngrok

```bash
# نافذة 2: شغّل ngrok
ngrok http 8000
```

**سترى:**
```
Session Status                online
Forwarding                    https://abc123.ngrok.io -> localhost:8000
```

### الخطوة 6: احفظ الرابط ومفتاح API

```
📝 احفظ هذه المعلومات:

🔗 الرابط: https://abc123.ngrok.io
🔐 المفتاح: your-secret-key-change-me
   (أو المفتاح الذي عيّنته في MT5_API_KEY)
```

### الخطوة 7: جاهز للاستخدام! 🎉

الآن يمكنك التحكم في MT5 من أي مكان!

---

## 📱 الاستخدام اليومي

### كل يوم، افعل هذا:

```bash
# 1. افتح Command Prompt
cd C:\path\to\ICT\mt5_middleware

# 2. شغّل الخادم
python remote_control_server.py

# 3. في نافذة أخرى، شغّل ngrok
ngrok http 8000

# 4. خلاص! جاهز للتحكم من الموبايل
```

### أو استخدم سكربت التشغيل السريع:

```bash
# ملف واحد يشغل كل شيء
start_cloud_sync.bat
```

---

## 🤖 الأوامر المتاحة

### عندما تتكلم مع Claude:

#### 1. إنشاء Expert Advisor:
```
"اعمل EA باستراتيجية RSI"
"أنشئ Expert Advisor باستراتيجية تقاطع المتوسطات"
"اعمل EA باسم MyStrategy باستراتيجية MACD"
```

#### 2. تشغيل Backtest:
```
"شغّل backtest على EURUSD"
"اختبر الـ EA على الذهب من 2024"
"شغّل اختبار مرئي على GBPUSD فريم الساعة"
```

#### 3. الأتمتة الكاملة (الأفضل):
```
"اعمل EA باستراتيجية RSI واختبره على EURUSD"
"أنشئ استراتيجية MA Crossover وشغّل backtest مرئي"
```

#### 4. قراءة النتائج:
```
"ايه نتائج آخر backtest؟"
"كم الربح والـ drawdown؟"
```

---

## 📡 نقاط النهاية (API Endpoints)

### للمطورين - يمكنك استخدام هذه مباشرة:

| Endpoint | Method | الوصف |
|----------|--------|-------|
| `/` | GET | الصفحة الرئيسية |
| `/health` | GET | فحص الحالة |
| `/strategies` | GET | الاستراتيجيات المتاحة |
| `/create-ea` | POST | إنشاء EA |
| `/compile-ea/{name}` | POST | ترجمة EA |
| `/backtest` | POST | تشغيل Backtest |
| `/full-automation` | POST | أتمتة كاملة ⭐ |
| `/results/{name}` | GET | قراءة النتائج |
| `/claude/command` | POST | أوامر Claude |

### مثال على طلب:

```python
import requests

url = "https://abc123.ngrok.io/full-automation"
headers = {"X-API-Key": "your-secret-key"}
data = {
    "name": "RSI_Strategy",
    "strategy": "rsi",
    "symbol": "EURUSD",
    "timeframe": "H1",
    "from_date": "2024.01.01",
    "to_date": "2024.12.31",
    "visual": True
}

response = requests.post(url, json=data, headers=headers)
print(response.json())
```

---

## 🔧 حل المشاكل

### المشكلة 1: الخادم لا يبدأ

```bash
# تأكد من تثبيت المتطلبات
pip install -r requirements.txt

# تأكد من وجود Python في PATH
python --version
```

### المشكلة 2: ngrok لا يعمل

```bash
# تأكد من تفعيل Auth Token
ngrok config add-authtoken YOUR_TOKEN

# جرّب إعادة التشغيل
ngrok http 8000 --region us
```

### المشكلة 3: MT5 لا يُكتشف

```python
# تأكد من مسار MT5 الصحيح
# افتح remote_control_server.py وعدّل:
MT5_PATH = "C:/Program Files/MetaTrader 5/terminal64.exe"
```

### المشكلة 4: خطأ في API Key

```bash
# تأكد من إرسال المفتاح في Header
curl -H "X-API-Key: your-key" https://abc123.ngrok.io/health
```

### المشكلة 5: الترجمة (Compile) تفشل

```
الحل: ترجم يدوياً:
1. افتح MT5
2. اضغط F4 (MetaEditor)
3. افتح ملف الـ EA
4. اضغط F7 (Compile)
```

---

## 🔐 الأمان

### نصائح مهمة:

1. **غيّر مفتاح API الافتراضي:**
```bash
# Windows CMD
set MT5_API_KEY=your-super-secret-key-here

# Windows PowerShell
$env:MT5_API_KEY="your-super-secret-key-here"
```

2. **لا تشارك رابط ngrok علناً:**
   - الرابط يتغير كل مرة تشغّل ngrok
   - احتفظ به خاصاً

3. **استخدم ngrok مدفوع للثبات:**
   - الحساب المجاني = رابط يتغير
   - الحساب المدفوع = رابط ثابت (subdomain)

4. **راقب الطلبات:**
   - الخادم يسجل كل طلب
   - راجع الـ logs بانتظام

---

## 📊 الاستراتيجيات المتاحة

| الاستراتيجية | الوصف | المعاملات |
|--------------|-------|-----------|
| `ma_crossover` | تقاطع المتوسطات | MA_Fast_Period, MA_Slow_Period |
| `rsi` | مؤشر RSI | RSI_Period, Overbought, Oversold |
| `macd` | مؤشر MACD | Fast, Slow, Signal |
| `rsi_ma` | RSI + MA مدمج | RSI_Period, MA_Period |

### إضافة استراتيجية جديدة:

```python
# في mt5_complete_automation.py
STRATEGY_TEMPLATES["my_strategy"] = '''
    // كود MQL5 هنا
'''
```

---

## 🎓 أمثلة عملية

### مثال 1: إنشاء EA بسيط

```
أنت: "اعمل EA باستراتيجية RSI"

Claude: يرسل →
POST /create-ea
{
    "name": "RSI_EA",
    "strategy": "rsi",
    "params": {"RSI_Period": 14}
}

النتيجة: ✅ تم إنشاء RSI_EA.mq5
```

### مثال 2: أتمتة كاملة

```
أنت: "اعمل EA واختبره على الذهب"

Claude: يرسل →
POST /full-automation
{
    "name": "Gold_Strategy",
    "strategy": "ma_crossover",
    "symbol": "XAUUSD",
    "timeframe": "H1",
    "visual": true
}

النتيجة:
✅ تم إنشاء Gold_Strategy.mq5
✅ تم الترجمة
✅ MT5 يعرض Backtest مرئي!
👀 شاهد الشاشة!
```

### مثال 3: قراءة النتائج

```
أنت: "ايه نتائج الاختبار؟"

Claude: يرسل →
GET /results/Gold_Strategy

النتيجة:
{
    "total_profit": 1250.50,
    "total_trades": 45,
    "win_rate": 62.5,
    "max_drawdown": 8.3
}
```

---

## 🚀 نصائح للاستخدام الأمثل

1. **اترك الجهاز مفتوحاً:**
   - الخادم و ngrok لازم يشتغلوا

2. **استخدم شاشة إضافية:**
   - لمشاهدة Backtest المرئي

3. **جدولة التشغيل:**
   - استخدم Task Scheduler لتشغيل الخادم تلقائياً

4. **راقب الأداء:**
   - MT5 + الخادم يستهلكون موارد

5. **احتفظ بنسخة احتياطية:**
   - ملفات الـ EA في MQL5/Experts

---

## 📞 الدعم

- **GitHub Issues:** [github.com/Ahmedyassen77/ICT/issues](https://github.com/Ahmedyassen77/ICT/issues)
- **التوثيق:** `/docs` على الخادم
- **الأمثلة:** ملف `examples.py`

---

## 🎉 مبروك!

الآن يمكنك التحكم في MetaTrader 5 من أي مكان في العالم!

```
📱 موبايلك → ☁️ Claude → 🌐 ngrok → 💻 جهازك → 📊 MT5
```

**استمتع بالتداول الذكي! 🚀**

---

*آخر تحديث: ديسمبر 2024*
*الإصدار: 1.0.0*
